# Otto Master assistant entry

This project uses `AGENTS.md` as the shared repository rule file. Read it first, then follow the document order in `ONBOARD.md`.

The current repository has completed Phase 1 (Runtime, Message contract and Message Bus). Do not infer that the remaining empty Python or web files are implemented; hardware, Web, SQLite and cloud integrations are still planned phases.
