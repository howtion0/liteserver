# Phase 2 SQLite施工契约

## 基本信息

- 日期：2026-09-18
- 当前版本：`0.1.0` → `0.2.0`
- 当前Phase：Phase 2 SQLite持久化与迁移
- Git迭代支线：`test0.2`
- GitHub基线：`origin/test0.1` → `e0097c4649322175356487f13d73fde078afdd09`

## 本轮唯一目标

建立SQLite持久化单一入口：连接生命周期、版本化迁移、设备/分组/消息/命令/结果/设置表、消息日志脱敏和受控写入；将Storage接入Runtime关闭顺序，确保在途消息在关闭前落盘。

## 允许修改范围

- `src/otto_master/storage/**`
- `src/otto_master/runtime.py`
- `src/otto_master/message_bus.py`（仅为Storage日志订阅提供必要的精确接入）
- `tests/test_storage.py`
- `tests/test_runtime.py`
- `pyproject.toml`、`uv.lock`
- `docs/DEV_PROGRESS.md`、`docs/MODULE_STATUS.md`、`docs/LOG.md`
- `docs/CONSTRUCTION_PLAN.md`（仅更新Phase 2验收勾选；保留其他来源不明的用户改动）
- 本文件

## 非目标

- 不接ESP32、MQTT、Web、SQLite REST API、真实云API或硬件。
- 不修改根目录 `README.md` 用户现有改动。
- 不实现复杂Repository领域业务；本轮只提供Phase 2所需的持久化基础和稳定测试接口。

## 输入与外部依赖

- ESP32：否
- 真实云 API：否
- 局域网：否
- Windows验证：否
- MQTT Broker：否
- EVA真机：否
- 数据库：临时SQLite文件与Runtime默认数据路径

## 验收矩阵

| 验收项 | 方法 | 结果 |
|---|---|---|
| 首次运行生成数据库 | `tests/test_storage.py::test_first_open_creates_database_and_schema` | PASS |
| 重复迁移幂等 | `tests/test_storage.py::test_migrations_are_idempotent` | PASS |
| 并发写入无锁错误 | `tests/test_storage.py::test_concurrent_message_writes_are_serialized` | PASS，100条 |
| 敏感字段脱敏 | `tests/test_storage.py::test_message_log_redacts_sensitive_fields` | PASS |
| Runtime关闭刷盘 | `tests/test_runtime.py::test_runtime_shutdown_flushes_in_flight_messages` | PASS，12条 |
| 全量静态/类型/单元回归 | `ruff check src tests`、`mypy src`、`pytest -q` | PASS，16 passed |
| 默认入口实际建库 | `python -m otto_master`后检查`data/otto.db`和schema version | PASS，version=1 |

## 风险与回滚点

- 风险：`aiosqlite`单连接并发写入、关闭超时和跨平台路径行为需要实际验证。
- 风险：消息payload必须保持JSON兼容，原始音频和密钥不能进入数据库。
- 回滚点：`test0.1` 的 `e0097c4649322175356487f13d73fde078afdd09`。
- 安全清理：不涉及设备动作，无需发送stop或确认idle。

## 实际结果

- 施工前基线：`test0.1` 已推送，且本地与远程哈希一致。
- `ruff check src tests`：PASS。
- `mypy src`：PASS，28 source files。
- `pytest -q`：PASS，16 passed。
- 默认入口：PASS，实际生成 `data/otto.db`，Ctrl-C后数据库关闭日志出现。
- 受影响的 Server 控制台需求文档改动未纳入本轮提交，保留在工作区。
