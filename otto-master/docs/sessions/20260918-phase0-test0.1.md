# Phase 0+1恢复基线施工契约

## 基本信息

- 日期：2026-09-18
- 当前版本：`0.1.0`
- 当前阶段：Phase 0文档 + Phase 1 Runtime与Message Bus
- Git迭代支线：`test0.1`
- GitHub基线：`origin/main` → `7064783ece974d19bb44aa82e8d58d0739c8c938`

## 本轮唯一目标

将现有已完成的 Phase 0/Phase 1 工程精确备份到新的 `test0.1` 远程支线，为 Phase 2 SQLite 施工提供可恢复基线。

## 允许范围

- 允许暂存和提交：`otto-master/**`
- 不纳入本轮：根目录 `README.md` 现有用户修改、`otto-master/.env`、虚拟环境、数据库、JSONL日志和固件二进制。
- 不执行：合并 `main`、tag、release、force-push。

## 输入与外部依赖

- ESP32：否
- 真实云 API：否
- 局域网：否
- Windows验证：否
- MQTT Broker：否
- EVA真机：否

## 通过标准

| 验收项 | 方法 | 结果 |
|---|---|---|
| Phase 1静态检查 | `uv run --project . --extra dev ruff check src tests` | PASS |
| Phase 1类型检查 | `uv run --project . --extra dev mypy src` | PASS |
| Phase 1单元测试 | `uv run --project . --extra dev pytest -q` | PASS，10 passed |
| 入口启动与退出 | `uv run --project . python -m otto_master`，Ctrl-C后观察停止日志 | PASS |
| 远程备份 | `git ls-remote --heads origin test0.1`，与本地HEAD比较 | 待运行 |

## 风险与回滚点

- 风险：当前 `otto-master/` 在原始仓库中尚未跟踪，本轮需确认敏感和运行产物不会进入提交。
- 回滚点：远程 `origin/main` 的 `7064783ece974d19bb44aa82e8d58d0739c8c938`。
- 安全清理：不涉及设备动作，无需发送stop或确认idle。

## 实际结果

- 施工前工作区存在根目录 `README.md` 用户修改；未纳入本轮。
- `ruff`、`mypy`、`pytest` 和入口启动/退出均已 PASS。
- 远程支线结果在提交和push后回填。
